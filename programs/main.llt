nat : type.

z : nat.
s nat : nat.

plus nat nat nat: bwd.

plus z N N.

% this has to be translated to:
%
% ---------------------------------   axiomatic
% I |- -- : plus(TT? z, T? N, T? N)


plus (s N) M (s P)
  <- plus N M P
  .

% this has to be translated to:
%
% I |- -- : plus(       T? N, T? M,        T? P)
% ----------------------------------------------   rule
% I |- -- : plus(TT? s. T? N, T? M, TT? s. T? P)
