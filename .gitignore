out/
*.*~
node_modules/

# bun artifacts — we stick with npm / package-lock.json for installs
bun.lockb
bun.lock

# Nix/direnv
.direnv/
result
result-*

# SolidStart build outputs
.output/
.vinxi/

# Tree-sitter generated files
lib/tree-sitter-mde/src/
lib/tree-sitter-mde/*.so
lib/tree-sitter-mde/*.wasm
lib/tree-sitter-mde/bindings/

.todo
.todo.md

# Benchmark comparison worktrees
.bench-compare/
.bench-history/

# Rust build artifacts
zk/target/
zk/spike/output/

# Claude Code sandbox bind-mount stubs
# The sandbox creates empty 444 files as mount points for ro bind mounts.
# They persist after session exit. Can't be deleted while session is active.
.bash_profile
.bashrc
.gitconfig
.gitmodules
.profile
.zprofile
.zshrc
.ripgreprc
.mcp.json
/HEAD
/config
/hooks
/objects
/refs
zk/HEAD
zk/config
zk/hooks
zk/objects
zk/refs

# fetched/generated fixture subdirs ONLY — checked-in fixtures (till-*.ill,
# goldens, .rules/.calc) track normally, no `git add -f` needed (round-15)
tests/fixtures/vmtests/
tests/fixtures/VMTests/
tests/fixtures/proof-trees/

# Claude Code config
.claude/settings.local.json
.claude/projects/
.claude/agents
.claude/commands
.claude/skills

# IDE (also sandbox stubs)
.idea
.vscode

# LaTeX build artifacts (doc/paper/*/)
doc/paper/**/*.aux
doc/paper/**/*.log
doc/paper/**/*.bbl
doc/paper/**/*.blg
doc/paper/**/*.out
doc/paper/**/*.toc
doc/paper/**/main.pdf
